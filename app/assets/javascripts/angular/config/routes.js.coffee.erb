'use strict'

convoApp = angular.module('convoApp')

convoApp.config(['$routeProvider', '$locationProvider', '$stateProvider', ($routeProvider, $locationProvider, $stateProvider) ->
  # Nothing here yet
])
