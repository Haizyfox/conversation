'use strict'

memeVineApp = angular.module('memeVineApp')

memeVineApp.config(['$routeProvider', '$locationProvider', '$stateProvider', ($routeProvider, $locationProvider, $stateProvider) ->
  # Nothing here yet
])
