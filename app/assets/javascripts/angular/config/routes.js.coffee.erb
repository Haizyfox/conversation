'use strict'

memeVine = angular.module('memeVine')

memeVine.config(['$routeProvider', '$locationProvider', '$stateProvider', ($routeProvider, $locationProvider, $stateProvider) ->
  # Nothing here yet
])
